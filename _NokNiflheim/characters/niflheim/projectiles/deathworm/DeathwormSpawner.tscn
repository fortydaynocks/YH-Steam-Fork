[gd_scene load_steps=13 format=2]

[ext_resource path="res://characters/BaseChar.gdshader" type="Shader" id=1]
[ext_resource path="res://projectile/DirProjectile.tscn" type="PackedScene" id=2]
[ext_resource path="res://_NokNiflheim/characters/niflheim/projectiles/deathworm/Deathworm.tscn" type="PackedScene" id=3]
[ext_resource path="res://mechanics/Hitbox.gd" type="Script" id=4]
[ext_resource path="res://_NokNiflheim/characters/niflheim/projectiles/wormscripts/hostspawner.gd" type="Script" id=5]
[ext_resource path="res://_NokNiflheim/characters/niflheim/effects/HitEffect.tscn" type="PackedScene" id=6]
[ext_resource path="res://_NokNiflheim/characters/niflheim/sounds/100_hit_grap_1.wav" type="AudioStream" id=7]
[ext_resource path="res://_NokNiflheim/characters/niflheim/sounds/107_throw_comeout.wav" type="AudioStream" id=8]
[ext_resource path="res://_NokNiflheim/characters/niflheim/effects/Portal.tscn" type="PackedScene" id=9]

[sub_resource type="ShaderMaterial" id=1]
resource_local_to_scene = true
shader = ExtResource( 1 )
shader_param/color = Color( 1, 1, 1, 1 )
shader_param/max_color_distance = 0.01
shader_param/super_dim = false
shader_param/use_outline = false
shader_param/outline_color = Color( 0, 0, 0, 1 )

[sub_resource type="SpriteFrames" id=3]
animations = [ {
"frames": [  ],
"loop": true,
"name": "default",
"speed": 5.0
} ]

[sub_resource type="GDScript" id=4]
script/source = "extends ObjectState

const FREE_AFTER_TICKS = 60

export  var _c_Projectile_Dir = 0
export  var move_speed = \"15.0\"

export  var _c_Homing_Options = 0
export  var homing = false
export  var homing_turn_speed = \"3.0\"
export  var homing_accel = \"1.0\"
export  var max_homing_speed = \"10\"
export  var start_homing = false
export  var lifetime = 99999
export  var relative_data_dir = false

export (PackedScene) var deathworm_scene
var wormtick = 0

var hit_something = false
var hit_something_tick = 0

func _frame_1():
	
	hit_something = false
	hit_something_tick = 0
	host.set_grounded(false)
	if homing:
		if start_homing:
			var dir = data[\"dir\"]
			var move_vec = fixed.normalized_vec_times(str(dir.x), str(dir.y), move_speed)
			host.apply_force(move_vec.x, move_vec.y)
		else :
			host.apply_force_relative(move_speed, \"0.01\")
	

func _tick():
	if wormtick >= 2:
		host.spawn_object(deathworm_scene, 0, 0, true, null, true)
		wormtick = 0
	
	wormtick += 1

	var pos = host.get_pos()
	host.update_grounded()
	if current_tick > lifetime:
		fizzle()
		host.hurtbox.width = 0
		host.hurtbox.height = 0
	elif not hit_something:
		var dir
		if not homing:
			dir = data[\"dir\"]
			var dir_x = fixed.mul(dir.x, str(host.get_facing_int())) if relative_data_dir else dir.x
			var move_vec = fixed.normalized_vec_times(dir_x, str(dir.y), move_speed)
	
			host.move_directly(move_vec.x, move_vec.y)
			host.sprite.rotation = float(fixed.vec_to_angle(dir.x, dir.y))
			host.set_facing(fixed.sign(dir_x))
		else :
			var target = host.obj_local_center(host.creator.opponent)
			var vel = host.get_vel()
			var current = fixed.normalized_vec(vel.x, vel.y)
			var desired = fixed.normalized_vec(str(target.x), str(target.y))
			var steering_x = fixed.sub(desired.x, current.x)
			var steering_y = fixed.sub(desired.y, current.y)
			var steer_force = fixed.normalized_vec_times(steering_x, steering_y, homing_turn_speed)
			var force_x = fixed.mul(current.x, homing_accel)
			var force_y = fixed.mul(current.y, homing_accel)
			if not fixed.eq(vel.x, \"0\"):
				host.set_facing(1 if fixed.gt(vel.x, \"0\") else - 1)
			host.apply_force(force_x, force_y)
			host.apply_force(steer_force.x, steer_force.y)
			host.apply_forces()
			
			host.update_data()
			var new_vel = host.get_vel()
			if fixed.gt(fixed.vec_len(new_vel.x, new_vel.y), max_homing_speed):
				var clamped_vel = fixed.normalized_vec_times(new_vel.x, new_vel.y, max_homing_speed)
				host.set_vel(clamped_vel.x, clamped_vel.y)
			host.sprite.rotation = float(fixed.vec_to_angle(fixed.mul(new_vel.x, str(host.get_facing_int())), new_vel.y))
	
	

func _got_parried():
	if homing:
		fizzle()

func fizzle():
	hit_something = true
	host.disable()
	terminate_hitboxes()
	hit_something_tick = current_tick
"

[node name="DeathwormSpawner" instance=ExtResource( 2 )]
material = SubResource( 1 )
script = ExtResource( 5 )
extra_state_variables = ""

[node name="Sprite" parent="Flip" index="0"]
rotation = 1.5708
scale = Vector2( 0.25, 0.25 )
frames = SubResource( 3 )

[node name="Default" parent="StateMachine" index="0"]
script = SubResource( 4 )
sprite_animation = "default"
anim_length = 2
enter_force_dir_x = "0"
enter_force_speed = "0"
timed_particle_scene = ExtResource( 9 )
timed_spawn_particle_tick = 200
sfx = ExtResource( 8 )
sfx_tick = 420
sfx_volume = -6.0
move_speed = "5"
homing = true
homing_turn_speed = "0.25"
homing_accel = "0.1"
max_homing_speed = "9"
lifetime = 200
relative_data_dir = false
deathworm_scene = ExtResource( 3 )

[node name="BodyHitbox" type="Node2D" parent="StateMachine/Default" index="0"]
z_index = 1000
script = ExtResource( 4 )
damage = 40
minimum_damage = 10
hitstun_ticks = 18
hitlag_ticks = 0
screenshake_frames = 8
hit_particle = ExtResource( 6 )
replace_hit_particle = true
hit_sound = ExtResource( 7 )
whiff_sound_volume = -6.0
hit_sound_volume = -6.0
dir_x = "1"
dir_y = "-0.5"
knockback = "8"
grounded_hit_state = "HurtAerial"
start_tick = 1
active_ticks = 1
always_on = true
looping = true
loop_active_ticks = 14
loop_inactive_ticks = 1
